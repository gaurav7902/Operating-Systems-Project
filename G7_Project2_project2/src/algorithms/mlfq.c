#include "../../include/scheduler_algorithms.h"

void runMLFQ(Process p[], int n, GanttEntry chart[], int *chart_size) {
    (void)p;
    (void)n;
    (void)chart;

    /* TODO (Team MLFQ): Implement MLFQ scheduling logic here. */
    *chart_size = 0;
}

#include "../../include/scheduler_algorithms.h"

void runEDF(Process p[], int n, GanttEntry chart[], int *chart_size) {
    (void)p;
    (void)n;
    (void)chart;

    /* TODO (Team EDF): Implement EDF scheduling logic here. */
    *chart_size = 0;
}
